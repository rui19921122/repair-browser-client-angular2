import { FilterSelectedDateFromMappedListPipe } from './filter-selected-date-from-mapped-list.pipe';

describe('FilterSelectedDateFromMappedListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSelectedDateFromMappedListPipe();
    expect(pipe).toBeTruthy();
  });
});
