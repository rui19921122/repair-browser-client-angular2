import { GetDataByIdPipe } from './get-data-by-id.pipe';

describe('GetDataByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GetDataByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
