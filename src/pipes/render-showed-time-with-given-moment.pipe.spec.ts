import { RenderShowedTimeWithGivenMomentPipe } from './render-showed-time-with-given-moment.pipe';

describe('RenderShowedTimeWithGivenMomentPipe', () => {
  it('create an instance', () => {
    const pipe = new RenderShowedTimeWithGivenMomentPipe();
    expect(pipe).toBeTruthy();
  });
});
