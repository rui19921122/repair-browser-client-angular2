import { DiffTimeWithStringFormatPipe } from './diff-time-with-string-format.pipe';

describe('DiffTimeWithStringFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new DiffTimeWithStringFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
