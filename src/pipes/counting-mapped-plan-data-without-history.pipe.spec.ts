import { CountingMappedPlanDataWithoutHistoryPipe } from './counting-mapped-plan-data-without-history.pipe';

describe('CountingMappedPlanDataWithoutHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new CountingMappedPlanDataWithoutHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
