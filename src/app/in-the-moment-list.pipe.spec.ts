import { InTheMomentListPipe } from './in-the-moment-list.pipe';

describe('InTheMomentListPipe', () => {
  it('create an instance', () => {
    const pipe = new InTheMomentListPipe();
    expect(pipe).toBeTruthy();
  });
});
